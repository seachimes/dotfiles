# Secret-scanning guard. Activate once with: pre-commit install
# Thereafter every `git commit` is blocked if a secret is detected.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
